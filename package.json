{
  "name": "ol-esbuild",
  "version": "1.0.0",
  "description": "Example using OpenLayers with",
  "scripts": {
    "build": "esbuild main.js --bundle --minify --sourcemap --outfile=bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.11.18"
  },
  "dependencies": {
    "ol": "^6.5.0"
  }
}
